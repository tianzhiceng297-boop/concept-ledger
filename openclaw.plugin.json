{
  "id": "concept-forge",
  "name": "Concept Forge",
  "kind": "plugin",
  "version": "2.0.0",
  "description": "Forge raw thoughts into implementable concepts. Captures metaphors & client jargon from brainstorming, decodes vague requirements, detects naming chaos & definition drift — and auto-builds a shared terminology dictionary your whole team can rely on.",
  "author": "Concept Forge Team",
  "configSchema": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "Optional project identifier for ledger isolation. Defaults to 'default' if not set."
      },
      "autoIntervene": {
        "type": "boolean",
        "description": "Whether the plugin should automatically intervene in the conversation when signals are detected. Defaults to true.",
        "default": true
      },
      "synonymThreshold": {
        "type": "number",
        "description": "Jaro-Winkler similarity threshold for synonym detection. Defaults to 0.85.",
        "default": 0.85
      }
    }
  }
}
